{
  "name": "king-day-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run --prefix client build",
    "prisma:generate": "prisma generate",
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "prisma": "^5.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
